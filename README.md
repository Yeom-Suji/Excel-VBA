![header](https://capsule-render.vercel.app/api?type=waving&color=gradient&height=200&section=header&text=Excel%20VBA&fontSize=50)

## _Trying to handle Visual Basic Analysis efficiently_ 📈

<img src="https://img.shields.io/badge/Excel&nbsp;VBA-217346?style=for-the-badge&logo=Microsoft&logoColor=white">
<br/>

😀 Data Digital Transformation -> RPA시대☘️☘️☘️
-> 자동화를 하려고 노력해야함, 가지고있는 Tool로도 자동화 할 줄 알아야함



😀 데이터를 의심하고 검증하라(데이터가 분석하기에 바람직 한가?)
1. 데이터가 정형화데이터인지 비정형데이터인지
2. 일반 데이터 범위인지 표개체인지 확인하기
	일반 데이터 범위를 표개체로 바꿔야함 [삽입 -> 표(CTRL + T), 표로 바뀐 후 표도구 있는지 확인]
	표개체를 일반데이터 범위로 바꾸는 방법 [표도구 -> 일반데이터변환]
		1. 표개체
			1. 동적 범위로 자동으로 잡아준다
			2. 필드 단위 계산: 구조적 참조
3. 데이터가 구조화 되어있는지 확인하기(별표5개)
	헤더가 셀병합이 안되어있어야함
	세로방향으로 데이터 쌓여있기
	중복있어도 비워놓지말고 채우기
4. 필드별 데이터 타입이 정확하게 잡혀있는지
	숫자 문자 날짜 수식 中 하나

😀 파워쿼리로 가공하는 표준 프로세스
일반데이터범위 -> 표체게 -> 파워쿼리로 불러가서 가공(텍스트로 바꾸고 날짜로 바꾸면 히스토리에 남길 수 있음) -> 위크시트로 가공
-> 원본 업뎃 -> 쿼리내에서 새로고침 -> 검증(원본의 데이터가 더 들어왔는지 새로고침해서 확인)
동료가 연결한 파워쿼리를 확인하려면? 데이터 -> 쿼리 및 연결
뒤에 필드를 더 추가하고 싶을 때? 리본메뉴에서 열추가 -> 사용자 지정열 -> 사용자 지정 열 수식 정해주기(ex. ="YSJ") -> 문법적으로 맞는지 확인 
결측치 제거하고 싶을 때? 파워쿼리창에서 마지막 열 아래 아이콘 -> 결측치나 네임error 없앰

😀 원본 데이터 설정 (원본 폴더나 원본 파일의 경로가 바뀌거나 이름이 바뀌면?)
쿼리 및 연결 경로가 바뀌어서 에러가 뜰때? 오류 더블클릭 -> 홈 -> 데이터 원본 설정 -> 경로 선택하고 원본 변경 
-> 찾아보기로 경로 재설정 -> 모두 새로고침

😀 엑셀 파일 통합
1. 특정 폴더 내에 있는 엑셀 파일
	데이터 가져오기 -> 파일에서 -> 파일 선택하고 -> 결합 내림 단추 -> 데이터 결합 및 변환 -> 첫번째 파일에서 시트 선택하고 확인
2. 시트 통합
	특정 파일 내, 여러 시트 존재 + 취합할 시트 이름 모두 다름 + 시트별 필드 이름, 순서, 개수 모두 같음
	파일 내, 모든 시트 데이터 취합하기
	새 통합문서 -> 리본 메뉴 -> 데이터 -> 데이터 가져오기 -> 파일에서 -> 통합 문서에서 -> 시트 취합할 파일 선택 -> 파워쿼리 들어가서
	-> ‘Data’ 필드를 제외한 나머지 필드들 열 제거 -> ‘확장‘ 아이콘 클릭 -> ‘원래 열 이름을 접두사로 사용’ 해제
	-> 1행 데이터를 헤더로 옮기기 : 파워쿼리 리본메뉴 -> 홈 -> ‘첫 행을 머리글로 사용’
	-> 나머지 헤더들 필터링 처리하기 : ‘Date’ 필드 내림버튼 클릭 -> ‘Date’ , ‘(Null)’ Uncheck
	-> 파워쿼리 리본메뉴 -> 홈 -> 닫기 및 로드 -> 워크시트로 쿼리 테이블 로드 완료
	-> 업데이트 검증하기 : 특정 파일 내, 시트 추가 > 쿼리 테이블에서 ‘새로 고침‘ > 업데이트 완료

😀 웹 스크래핑
데이터 -> 데이터가져오기 -> 기타데이터 -> 웹
시간마다 자동으로 새로고침 되는 쿼리? 데이터 -> 속성 -> 이름옆 아이콘 -> 시간마다 새로고침
오류가 날 때? 데이터 -> 데이터 가져오기 -> 쿼리 옵션 -> 개인정보 -> 무시
데이터 로딩속도가 느릴 때? 데이터 -> 데이터 가져오기 -> 쿼리 옵션 -> 데이터 로드에 캐시 지우기

💥 참고
재생목록에 데이터처리 #36번 (엑셀 매게변수)

😀 양질의 데이터 수집
KOSIS
서울열린데이터광장
부산광역시 빅데이터플랫폼
빅카인즈/썸트렌드
Kaggle
Gapminder
KDX 한국데이터거래소

💥 미션1
현재 통합문서에 '시청자수 Top20' 데이터 테이블을 새로운 시트로 웹스크래핑

💥 미션2
시트통합 진행 -> 검증까지!

😀 크로스테이블 구조화
크로스테이블은 보고서 용도로는 바람직함 그러나 데이터 분석 용도 X -> 언피벗 시켜야함
언피벗 어떻게해? 파워쿼리에서 언피벗할 영역 전체 선택하고 우클릭 -> 언피벗하기
열 병합하고 싶을 때? 병합할 열 2개 선택하고 병합하기, 알맞은 기호 넣기
열 추가하고 싶을때? 사용자 지정 열 추가

💥 미션3
데이터 구조화하고 검증까지 해보기

2023-03-27
😀 데이터 분석이 왜 중요한가
데이터 분석을 톻해 인사이트 도출 -> 기업 가치창출, 의사결정
	-코로나시대 불확실서에 대한 자각 -> 조금이라도 예측하고 싶어 -> 양질의 데이터 플랫폼 다수 등장
	-빅데이터 분석을 위한 소프트웨어 등장(ex 테블로나 엑셀 파워쿼리)
	-예전에는 재정적 촉이나 재능으로 회사가 욺직였으나 이제는 데이터 분석을 통해 의사결정, 설득, 조직변경을 함 

😀데이터 취합(파일통합x, 시트통합x)
1. UNION(추가)
	흩어진 데이터 테이블들을 파워쿼리가 인지하도록 '연결전용'으로 만들어준다
	일반데이터 범위 -> 표 개체-> 파워쿼리로 가져가기 -> 연결전용으로 로드 -> 쿼리 우클릭 -> 추가 -> 합친거 새 시트에 뿌려주기
2. JOIN(병합)
	- 표 개체 -> 파워쿼리로 가져가기 -> 워크시트로 로드 -> 연결전용
	- 연결전용 완료된 쿼리에서 -> 우클 -> 병합 -> primary key 선택 -> 조인 옵션 선택 -> 확인
	- 검증!!
	- 새로운 데이터가 원본에 누적되면? 모두 새로고침으로 자동화
	- 완전 외부 조인에서 문제가 생겼다(null값을 처리하고자 할 때)? 쿼리창에서 조건열 추가 -> 조건 걸어주기
	  (합치고자 하는 A열이름을 넣고, 연산자 같음, 값은 null, 출력열은 합치고자하는 B열 
	    -> 다음 조건 열이름이랑 출력 내용 바꿔서 넣기, 기타에는 A열이름 넣기)

😀 데이터 조인하기
깨끗한 통합문서 열기 -> 파워쿼리로 데이터 가져오기 -> 파워쿼리로 편집 -> 워크시트로 로드 : 연결 전용 -> 데이터 테이블들 조인(병합) 
-> 최종 취합 쿼리테이블만 워키시트로 로드

😀시각적분석(피벗테이블과 조건부서식은 환상의 조합)
1. 데이터 가공 전처리
	raw data 보고 판단하기 (4가지 기준 바람직한가 아닌가)
	1. 데이터 구조화(데이터가 아래로 쌓여있나)
	2. 데이터 타입(값이 적절한 테이터 타입인지)
	3. 리본메뉴에서 표도구가 없으면? 일반 데이터범위! 
	   -> 표개체로 바꿔라 -> 파워쿼리로 가져가기 -> 편집 -> 워크시트로 로드 
	   -> 기본분석: pivot table -> 정석은 쿼리내에 셀포인터 선택하고 리본메뉴 삽입 피벗테이블
	   (새로운 측정값을 만들 때는 피벗테이블 만들고 AllData 우클릭 -> 측정값 추가 -> Sum(Alldata[~])/sum~~) -> 시각적분석(가독성)
2. 시각적분석(가독성) -> 디자인탭 -> 보고서레이아웃 -> 테이블 형식 -> 조건부서식에서 시각화 
	   여기까진---- 정적분석 > 동적분석으로 바꾸자
	1. 동적 분석 -> 슬라이서
	2. 리본메뉴 보기 -> 눈금선 제거
	3. 원천데이터의 누적으로 업뎃하려면? 대쉬보드까지 순차적으로 새로고침 이행
	   (매크로 버튼 만들기는 데이터 시각화 스파크라인 시각화 11-5번 영상 16분짜리)
3. 텍스트 마이닝: 빅카인즈, 썸트렌즈
4. 데이터 분석
	1. 기본분석: 데이터 있는 그대로 집계
	2. 통계분석: 데이터의 미래예측
	3. 가상분석: 데이터들의 최적의 조합
	머신러닝
	텍스트 마이닝
5. 시각적분석(고도화하기)
	-안정성있는 설계: Error 없이
	-처리속도: 빠르게 처리
	데이터들 가공해서 조인해 놓고 데이터 저장을 연결만 만들면서 [데이터 모델에 이 데이터 추가] 체크 -> 처리속도 빨라지고 파일 크기 작아짐
	+) 왜냐? 연결해놓은걸 피벗테이블로 만들기 위해서
	1. 삽입에 피벗테이블 만들기 -> 데이터 모델의 피벗 테이블에서 확인
	2. 원하는 값들을 피벗시트에서 뽑아 여러개 피벗테이블들 만들어놓기(테이블들은 서로 띄워서 만들기)
	3. 대시보드 시트에서 피벗테이블 가져오는 방법? = 쓰고 피벗 시트에서 칸 선택하고 Enter
	4. 피벗차트 만들기 (막대 가독성 높은 데이터 계열 서식? 계열 겹치기0 간격 너비 100, 시계열 차트 = 꺾은선형 차트라는 뜻)
		슬라이서는 무조건 우클릭 -> 보고서 연결
		도형 서식 -> 속성 -> 사이즈 변하지 않음
		프린트해야할때 보기 메뉴에서 페이지 확인하기

💥과제
엑셀 디자인 채널 들어가서 엑디상회 #5번 15분짜리 보기

2023-03-28
😀 시각적 분석: 태블로, Power Bi
1.시각화?
2. 현재 현업의 많은 데이터들이 엑셀의 수식(함수)을 사용해서 설계 -> 데이터베이스화 x -> 새로운 보고서 작성
3. 가장 어려운 점: 데이터 구조화	

😀 Open Api 방식으로 시각적 분석
고용행정통계
경기도 군포시, 23년 1~2월, 남여별, 25~29세, 실업급여 분석해보자.
- 파워쿼리 중 훼손된 원본? 파워쿼리 원본 톱니바퀴에서 맨밑 949 한국어
- 무조건 서식 슬라이서 가서 속성 변하지 않음

😀 통계 분석
KOSIS 자료, 스트레스 인지율과 자살 시도율이 서로 상관관계가 있는지 알고 싶다.
만약에 이 두 변수가 상관관계가 있다면 스트레스 인지율 변화에 따른 자살 시도울을 예측하고 싶다.
1. 준비단계
	개발도구 리본을 꺼내고 -> 엑셀 추가기능 -> 분석도구 팩 (그러면 데이터 리본에 데이터 분석으로 들어옴)
2. 산포도
	범위잡고 삽입에 분산형 차트 내림버튼 그중에 산포도
	추세선 추가로 수식을 차트에 표시(회귀식), R^2값 표시(결정계수)
3. 상관분석
	데이터 리본 -> 데이터분석에 상관분석
	1-<상관계수<1
		-1에 가까울수록 음의 상관관계
		1에 가까울수록 양
		0은 아무 관련 없음
4. 회귀분석
	스트레스 인지율 변화에 따른(독립변수) -> 왼쪽에 적는게 국룰
	자살 시도율을 예측해보고 싶다(종속변수) -> 오른쪽 배치
	결정계수는 0<<1
		1에 가까울수록 좋음 (0.935 이면 93%의 설명력을 가지고 있다)
		파이썬에서는 r 제곱값 R^2
	-단순회귀분석: 독립변수1개, 종속1개
		회귀식(회귀모형): y= ax + b 꼴
		회귀식으로 다음 값을 예측 가능
	-다중회귀분석
	-로지스틱...

😀 VBA를 활용한 자동화 (노가다 < 파워쿼리 < VBA)
파워쿼리의 한계.. KPI보고서 같은 지독한 보고서들은 VBA로 해결해야함
1. 매크로(macro): 반복되는 작업을 기록해 두었다가 필요시 단축키 또는 버튼만 누르면 해당 작업을 자동으로 실행할 수 있는 기능
	매크로 기능이 들어있는 파일을 ‘매크로 파일’이라고 부름
2. VBA(Visual Basic for Application): MS사의 윈도 오피스 응용 프로그램용 프로그래밍 언어
3. VBE(Visual Basic Editor): VBA로 코딩하기 위한 편집창
4. 준비단계로 최적화하기
	코딩 전용 글꼴: 네이버, 무료 배포 -> 나눔고딕코딩 설치하기
	개발도구에서 VBE, 아니면 우클 코드보기, 아니면 alt +f11
	필수창 3개는 항상 띄워놓기 (프로젝트창(프로젝트탐색기), 속성창, 코드창)
	보기 메뉴에서 도구모음 -> 편집툴도 꺼내기
	도구 메뉴 -> 옵션 -> 편집기에서 변수 선언 요구(변수에 에러나면 친절하게 알려줌)
	도구메뉴 -> 옵션 -> 편집기 형식 -> 글씨체 나눔고딕코딩(한글)
5. 모듈 만들기
	파일 정확하게 선택하고 모듈 삽입
6. sub 프로시저명 -----------------> 족보형태☆☆☆☆☆☆☆☆☆☆☆☆
	-프로시저 이름과 변수 이름 빼고 코드는 모두 소문자로 쓰자
		- 엑셀 개체 모델: 족보
			가장 상위의 개체는 application(엑셀 프로그램 전체) > workbooks collection(엑셀 통합 문서들) 
			> worksheets collection(시트) > range/cells(셀)
		ex) Sub Object_1()
  		      Application.Workbooks("개체모델.xlsm").Worksheets("취합").Range("a1") = "SJY"
		      Application.Workbooks("개체모델.xlsm").Worksheets("취합").Cells(2,1) = "SJY" -> Cells(2, "a")도 가능
 			    End Sub
			어플리케이션 솔직히 생략 가능
7.매크로 단추 넣기
	개발도구 -> 삽입 -> 단추 -> 설정할때 파일이름
	오류는 디버그로 확인 -> 파란 상자 눌러서 노란불 끄고 -> 디버깅하기
	시트의 이름이 변경돼서 매크로 단추에 오류가 날때? Sheet1.Range("a1:c10") = "Power Query" 처럼 VBA가 인지하는 이름으로 바로 넣기
8. Range 개체 핸들링
	Sheet1.Range("a1") = "연진아, 나 오늘 VBA 코딩했다"
	Sheet1.Cells(1, 52) = "재준"
	Sheet1.Cells(1, "az") = "재준"
9. End 속성
    Sheet2.Range("d1").End(xlToLeft).Select
    Sheet2.Range("a1").End(xlToRight).Select
    Sheet2.Range("d10").End(xlUp).Select
    Sheet2.Range("d1").End(xlDown).Select
 = Sheet2.Range("d1").End(1).Select
    Sheet2.Range("a1").End(2).Select
    Sheet2.Range("d10").End(3).Select
    Sheet2.Range("d1").End(4).Select
    	- 해당 열에 빈 세링 존재할 때 마지막 셀 선택하는 코드☆☆☆☆☆
		Sheet2.Cells(Rows.Count, "a").End(3).Select -> 3 or xlUp -> 외울 때 "세즈로즈 카운트에이 엔드 엑셀업"
10. 실행 바로가기
	- f5는 마지막 코드 실행
	- f8은 위에서부터 순서대로 실행	
11. Range 개체를 가지고 연속된 범위 설정
	Sheet2.Range("a1").CurrentRegion.Select
	Sheet2.Range("a1").CurrentRegion.Copy Sheet2.Range("k1") ------> 범위 설정한거 복사 붙여넣기
	ex) Sheet2.Range("a20:d40") = "현업 데이터 분석가"
   	    Sheet2.Range("a20:d40").CurrentRegion.Copy Sheet2.Range("k20")
- 데이터가 있는 연속된 범위 바로가기: ctrl + shift + 8
- 범위안에 모두 같은 값 넣기: ctrl+enter

12. 메서드: 실행 명령 -----------------> 지워 카피해 선택해
☆☆☆☆☆☆☆☆☆☆개체.메서드
	1. 지우기 예제(3가지)
    		Sheet3.Range("a8:a9").Delete ->  아래쪽 데이터가 위로 올라가서 셀의 주소가 바뀌어 위험하다. 근데 많이 씀
    		Sheet3.Range("a5:a7").ClearContents -> 지정 셀 데이터만 지워지고 서식은 안지워짐
    		Sheet3.Range("a1:a4").Clear -> 셀 데이터와 서식 모두 지워짐. 아래쪽 데이터가 위로 올라가지 않음
💥 참고
최준선 VBA 바이블 책 추천


2023-03-29
13. 속성: 개체의 속성 표현 ------------> 색깔, 크기 함 바꿔봐
☆☆☆☆☆☆☆☆☆☆개체.속성 = 속성값
	1. 색칠하기(3가지)
		Sub Property_1()
			Sheet4.Range("a1:a4").Interior.Color = vbYellow ---> 비비드해서 안예쁨
			Sheet4.Range("a5:a7").Interior.ColorIndex = 24 ----> 1~56
			Sheet4.Range("b5:b7").Interior.ColorIndex = 44
			Sheet4.Range("c5:c7").Interior.ColorIndex = 45
			Sheet4.Range("d5:d7").Interior.ColorIndex = 46
    			Sheet4.Range("a2").Interior.Color = RGB(123, 25, 11) ----> 원하는 색 쓸 수 있음
14. With문으로 코드 효율화(처리속도↑)
	Sub with_1()
		With Sheet1
		.Range("a1").CurrentRegion.Interior.ColorIndex = 44
		.Range("a1").CurrentRegion.Copy .Range("f1")
		End With
	End Sub
		With Sheet1
		.Range("a1").CurrentRegion
		.Range("a1", .Range("a1").End(xlDown))
		.Range("c1", .Cells(Rows.Count, "c").End(3))
		End With
		With Sheet1.Range("a1") 
		.Font.Bold = True
		.Font.Name = "돋움체" 
		.Interior.ColorIndex = 32
		End With
15. 변수: 값을 저장하는 값
	1. 일반변수
		변수는 프로시저 밑에서 정의해 주어야함, long 은 숫자(integer는 쁠마 삼만이천), string은 문자
		Sub variable_1()
  		   Dim i As Long
  		   Dim MyStr As String
   			 With Sheet2
    			    i = 1
    			    MyStr = "우크라이나"
    			    .Range("a1") = i + 10
     			    .Range("a2") = MyStr & "에 평화를"
			  End With
		End Sub
	2. 개체변수
		as 뒤에는 개체의 이름을 적으면 됨, 그리고 개체변수는 앞에 무조건 Set써주기, select 대신에 개체변수 설정
		Sub Variable_2()
 		   Dim rng As Range
  		     Set rng = Sheet3.Range("a1").CurrentRegion
 		     rng.Select
		End Sub
		Sub Variable_2()
 		   Dim rng As Range
 			With rng
   			  .Interior.ColorIndex = 44
			  .Copy .Range("h1")
 			End With
		End Sub
	3. 배열변수(처리속도↑)
	4. 사용자지정변수: 첨부터 끝까지 개발자가 세팅하는 변수
	5. 지역변수와 전역변수
16. 변수 검증
	1. 일반변수에 들어온 값 확인하기 (2가지)
		-f8 + 양손: 실행시키고 마우스로 lR 가리키기
		-msgbox lR: 실행하면 메세지로 뜸
		Sub CheckVariable()
		   Dim lR As Long, lR_1 As Long
			With Sheet1
			  'A열에 데이터가 있는 마지막 셀의 행번호를 lR 변수에 받아라 -> 행번호는 row이고 일반변수임(숫자)니까 Set안함
 			  lR = .Cells(Rows.Count, "a").End(3).Row
			  MsgBox lR
			  'B열에 데이터가 있는 마지막 셀의 행번호를 lR_1이라는 변수에 받아라
			  '그런 다음, lR_1변수에 들어온 값이 정확한지 확인하는 코드를 생성하라
			  lR_1 = .Cells(Rows.Count, "b").End(3).Row
			  MsgBox lR_1
			End With
		End Sub
	2. 개체변수에 들어온 값 확인하기 (2가지)
		-개체변수.select
		-조사식창 -> 중단점 설정하고 조사식창으로 확인
		Sub CheckVariable()
		   Dim rng As Range
			With Sheet1
			Set rng = .Range("a1").CurrentRegion
			rng.Select
			End With
		End Sub
17. if 조건문
18. 반복문
	Sub Result()
		With Sheet1
		'for 변수명 = 변수의 시작값 to 변수의 종료값
		Dim i As Long
			For i = 2 To 10
 			   If .Cells(i, "b") >= 500 Then
			       .Cells(i, "c") = "A"
 			   ElseIf .Cells(i, "b") >= 400 Then
   			        .Cells(i, "c") = "B"
    			ElseIf .Cells(i, "b") >= 300 Then
     			      .Cells(i, "c") = "C"
    			ElseIf .Cells(i, "b") >= 200 Then
     			      .Cells(i, "c") = "D"
   			 Else
   			    .Cells(i, "c") = "FFFFFF"
  			  End If
			Next
		    End With
	End Sub
19. 실무버전으로 업그레이드
	1. 매크로 버튼 꼭 만들기
	2. For i = 2 to 10 처럼 10 -> 하드코딩 하지 말기
		Sub Result()
		Dim i As Long, lR as long
		With Sheet1
			lR  = .cells(rows.count,"a").end(3).row
			For i = 2 To lR
	3. 기존 데이터를 지우고 매크로가 실행되도록 해야함
		- 기존데이터 지우기(2가지)
			+ .range("c2:c" & lR).clearcontents
	4. Application.ScreenUpdating = False/True
	   처리 속도를 올려줌, 마지막 End sub 전줄에서 True 로 끝내줘야함!!!!


😀 VBA 매크로
1. 일반 매크로: 맨 땅에 헤딩
2. 자동매크로: 엑셀 워크시트 창에서 메뉴 클릭을 통해 작업하는 과정을 엑셀이 자동으로 VBA언어로 바꾸는 매크로
	- 처리속도가 어마무시하게 느리다
	- 자동매크로로 기록이 안되는 작업들이 많다----> 99%는 자동매크로는 실무에서 사용하면 안됨
	   ex) 출력 매크로 개발자 느낌으로 수정하기


😀 회귀분석(regression)
1. 단순 회기분석: 독립변수1, 종속변수1
	1인당 국민 총소득 변화에 따른 운전면허소지자 수를 과학적으로 예측
	회귀식(회귀모형) = ax + b
2. 다중회귀분석: 독립변수 여러개, 종속변수 1

💥 미션 
회귀분석 매크로 버튼을 클릭했을 때 분석 도구와 분석도구-VBA툴이 활성화 되는 코드까지 현재 매크로에 추가하라

😀 로직을 세우고 결과물을 그림으로 그려봐, 반복되는건 뭐가있지?
Sub RemoveRows()
특정한 조건을 만족하는 행 제거
Dim lR As Long, i As Long
Application.ScreenUpdating = False
With Sheet3
lR = .Cells(Rows.Count, "a").End(3).Row
뒤에서 부터 지우면 위에 행 번호가 바뀌지 않음, step뒤에가 증가 폭
For i = lR To 2 Step -1
만약에 셀이 Bentley로 시작하면 then, *Bentley* 포함되면
If .Cells(i, "a") Like "Bentley*" Then
Bentley로 시작하는 셀이 포함된 전체 행을 삭제하겠다
.Cells(i, "a").EntireRow.Delete
End If
Next
End With
Application.ScreenUpdating = True
End Sub

💥 참고
'처리속도 2배 빨리하기 유튜브 VBA영상에 8번영상 유니온


😀 패턴코드 활용 매크로 개발


💥 미션
회귀분석 매크로 버튼을 클릭했을 때
분석도구, 분석도구-VBA 툴이 활성화되는 코드까지
현재 매크로에 추가하라!


☘️데이터 분석이 왜 중요한가?
데이터 분석을 통해 인사이트 도출 > 기업 가치창출, 의사결정
- 양질의 데이터 플랫폼 다수 등장
- 빅데이터 분석을 위한 소프트웨어 등장
- 데이터 분석, 설득, 조직 

🔸시각적분석 : 태블로 , Power Bi
1. 시각화?
2. 현재 현업의 많은 데이터들이 엑셀의 수식(함수)을 사용해서 설계 > 데이터베이스화 X > 새로운 보고서 작성X
3. 가장 어려운 점 : 데이터 구조화

🔸Open API 방식으로 시각적분석
경기도 군포시, 23년 1~2월, 남여별, 25~29세 , 실업급여 현황 분석
과제 제출
엑셀 파일 > 압축(zip) 해서 > 공유 폴더 업로드


🔸코딩 시 : 동료의 관점에서 디자인, 코딩하라
1. 안정성
2. 처리속도
   - with 문
   - 변수 활용
   - 배열변수 활용
   - 엑셀 기능 활용
3. 로직?
   - 결과물을 그림으로 그려봐라
   - 반복되는 패턴 : 행 반복? 열 반복?
4. 동료의 엑셀 라이프 스타일까지 고려해서 코딩.
       
🔸비교연산자
이상: >=
이하: <=
초과: >
미만: <
같다: =
같지않다: <>

🔸실무버전 업그레이드
1. 매크로 버튼으로 빼기
2. 데이터가 있는 마지막셀의 행번호 > 변수
3. 기존 데이터 지우기
4. Application.ScreenUpdating = False / True
5. 처리속도 체크 (4줄)

☘️ 마지막 과제
- 주문 시트에 데이터 가져오기
- Dashboard 시트에 : 피벗 테이블(고객세그먼트별, 지역별) + 차트 그려지기
- 주문 시트에 매크로 버튼 생성
- 위 3가지 항목 필수
- 추가로 고도화 하시는 것도 가능
